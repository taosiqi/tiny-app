{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Permissions required by the main compression window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
